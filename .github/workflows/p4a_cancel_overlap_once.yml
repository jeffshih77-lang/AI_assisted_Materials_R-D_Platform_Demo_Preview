name: P4-A cancel overlapping repair runs once
on:
  push:
    paths:
      - '.github/workflows/p4a_cancel_overlap_once.yml'
permissions:
  actions: write
  contents: read
jobs:
  cancel:
    runs-on: ubuntu-latest
    steps:
      - name: Cancel overlapping P4-A runs
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          for run in 34675954249 34671348649; do
            echo "cancel $run"
            gh api --method POST "repos/$REPO/actions/runs/$run/cancel" || true
          done
