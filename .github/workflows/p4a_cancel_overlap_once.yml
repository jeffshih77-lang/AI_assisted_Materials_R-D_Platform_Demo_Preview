name: P4-A cancel overlapping repair runs once
on:
  push:
    paths:
      - '.github/workflows/p4a_cancel_overlap_once.yml'
permissions:
  actions: write
  contents: read
jobs:
  cancel:
    runs-on: ubuntu-latest
    steps:
      - name: Cancel superseded queued 20-page matrix after preserving successful artifacts
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          for run in 34686421608; do
            echo "cancel $run"
            gh api --method POST "repos/$REPO/actions/runs/$run/cancel" || true
          done
