name: P2D Q030 COCO 20140319 attachment probe frozen

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Probe frozen after successful public anonymous GET in run 34748667507.'
